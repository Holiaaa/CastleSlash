#pragma once
#include "Utils.hpp"

/*
 *
 * Les structure mconfig sont composée comme ceci:
 * > ground : Fichier terrain, on peut marcher. (id=0)
 * 
 */

struct mconfig {
    text_t ground;
};